# Server
PORT=4000
CORS_ORIGIN=http://localhost:5173

# Postgres
DB_HOST=localhost
DB_PORT=5432
DB_USER=username
DB_PASS=yourpassword
DB_NAME=mydb

# Simulator ranges (optional overrides)
TEMP_MIN=22.5
TEMP_MAX=35.0
HUM_MIN=30.0
HUM_MAX=70.0
TICK_MS=5000